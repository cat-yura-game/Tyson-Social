{
  "name": "zashugannyy-kot-ai-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-assets.mjs",
    "dev": "node scripts/sync-assets.mjs && wrangler dev",
    "deploy": "node scripts/sync-assets.mjs && wrangler deploy",
    "check": "node scripts/sync-assets.mjs && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
