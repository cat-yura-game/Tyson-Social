# Tyson Social — native iPhone client

This folder contains the SwiftUI client for the Tyson Social beta. The app talks to the existing Tyson API; the website is not embedded in the native UI.

The initial beta targets iOS 26 and keeps a semantic fallback for earlier supported iOS versions. Build and signing are handled by the GitHub Actions workflow.
